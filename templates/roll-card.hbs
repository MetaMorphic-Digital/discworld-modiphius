<div class="discworld">
  <header class="flexcol trait-quote">
    <q>{{trait.name}}</q>
    <cite>{{actor.name}}</cite>
  </header>
  <!-- From Foundry -->
  <div class="flexrow dice-roll">
    {{#each rollers as |rollerI18nString roller| }}
      <div class="dice-result">
        <button class="{{roller}}">
          {{#compare roller "player" }}
            {{localize "Help"}}
          {{else}}
            {{localize rollerI18nString}}
          {{/compare}}
          <i class="fas fa-dice"></i>
        </button>
        <div class="dice-formula">{{../term}}</div>
        {{!-- djlint:off H021 --}}
        <div class="dice-tooltip" style="display: none;">
          {{!-- djlint:on --}}
          <section class="tooltip-part">
            <div class="dice">
              <header class="part-header flexrow">
                <span class="part-formula">{{../term}}</span>
                <span class="part-total">{{../result}}</span>
              </header>
              <ol class="dice-rolls">
                <li class="roll die {{../term}}">{{../result}}</li>
              </ol>
            </div>
          </section>
        </div>
        <h4 class="dice-total">{{../result}}</h4>
      </div>
    {{/each}}
  </div>
</div>
